apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-template-app.fullname" . }}
  labels:
    {{- include "helm-template-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-template-app.selectorLabels" . | nindent 4 }}
